# what happened
```r
$ go mod init github.com/nuoxoxo/nn/tree/main/g8-gin-api  
$ go get github.com/gin-gonic/gin
$ go run .
$ curl localhost:10086
$ curl localhost:10086/studs
$ curl -X PUT -d stuff http://localhost:10086/
```